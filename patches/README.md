# patches

Prefer scripted overlays from `scripts/apply-local-mods.py`.

Use `.patch` files only for stable upstream regions. Apktool 3.x is moving quickly, so direct line-based patches against parser internals are expected to drift.
